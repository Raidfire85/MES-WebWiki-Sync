{
  "name": "mes-webwiki-sync-tool",
  "private": true,
  "version": "1.0.0",
  "description": "MES WebWiki sync CLI (build deps for tsc; entry point is publish.cjs)",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
